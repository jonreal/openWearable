*.DS_Store
*/bin
build/
*.swp
*/datalog
tags
types_c.taghl
types_matlab.taghl
*.qmake*
*.o
*.app/*
moc_*
**/rotation_display.app/*

# Internal working notes (todo / planning / build journals / board inventory) — never published.
# Mirrors the am64x branch convention: docs/ is public, notes/ stays local.
notes/

# Library build outputs
library/lib/
library/build/

# Host tool binaries
utils/logdecode/logdecode
