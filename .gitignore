*.DS_Store
*/bin
build/
*.swp
*/datalog
tags
types_c.taghl
types_matlab.taghl
*.qmake*
*.o
*.app/*
moc_*
**/rotation_display.app/*
**/library/lib

# Host tool binaries
utils/logdecode/logdecode

# Internal-only, local working files (todo / planning / build journals / board+host
# specifics). Lives inside the repo dir for convenience but is NEVER published. Public
# docs go in docs/ ; the audience split is intentional — see docs/README.md.
notes/

# TI SDK patch-overlay: the *recipe* (MANIFEST, patches/, build scripts) is tracked under
# tidl/vision-apps/ ; the multi-GB TI source checkouts and build outputs are not.
tidl/vision-apps/src/
tidl/vision-apps/out/
